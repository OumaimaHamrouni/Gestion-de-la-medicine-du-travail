{% extends 'base.html.twig' %}

{% block title %}Evaluation{% endblock %}

{% block body %}
    <h1>Evaluation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation.id }}</td>
            </tr>
            <tr>
                <th>Impact</th>
                <td>{{ evaluation.impact }}</td>
            </tr>
            <tr>
                <th>Freqance</th>
                <td>{{ evaluation.freqance }}</td>
            </tr>
            <tr>
                <th>Evaluation</th>
                <td>{{ evaluation.evaluation }}</td>
            </tr>
            <tr>
                <th>ID_PT</th>
                <td>{{ evaluation.IDPT }}</td>
            </tr>
            <tr>
                <th>ID_R</th>
                <td>{{ evaluation.IDR }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evaluation_index') }}">back to list</a>

    <a href="{{ path('evaluation_edit', {'id': evaluation.id}) }}">edit</a>

    {{ include('evaluation/_delete_form.html.twig') }}
{% endblock %}
