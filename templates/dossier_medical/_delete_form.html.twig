<form method="post" action="{{ path('dossier_medical_delete', {'id': dossier_medical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossier_medical.id) }}">
    <button  class="btn btn-primary btn-danger color-block mb-0.5 mx-auto z-depth-1 btn-rounded btn-lg"><i class="fa fa-trash" aria-hidden="true">  SUPPRIMER </i> </button>
</form>

